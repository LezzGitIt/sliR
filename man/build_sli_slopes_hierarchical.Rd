% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/slopes.R
\name{build_sli_slopes_hierarchical}
\alias{build_sli_slopes_hierarchical}
\title{Per-cell SMA allometric slopes, with reliability-gated fallback}
\usage{
build_sli_slopes_hierarchical(
  df,
  Append = Append,
  Mass = Mass,
  control = character(0),
  n_min_cell = 50,
  n_min_marginal = 100,
  cor_min = 0.3,
  cor_p_max = 0.05,
  unknown_codes = c("Unk", "U", "Unknown")
)
}
\arguments{
\item{df}{A data frame of individual measurements.}

\item{Append, Mass}{Unquoted column names for appendage length and body mass.
\strong{These default to columns literally named \code{Append} and \code{Mass}.} If your
columns are named otherwise, pass them explicitly (e.g. \verb{Append = wing, Mass = mass}); an informative error is raised if the columns are absent.}

\item{control}{Character vector of \strong{0, 1, or 2} grouping columns. Each
must be character or factor. Level 1 (the combined cell) is only
attempted with exactly two; with \code{character(0)}, every row resolves to
the pooled slope.}

\item{n_min_cell}{Minimum \code{n} for level 1 (the combined cell). Defaults to
\code{50}.}

\item{n_min_marginal}{Minimum \code{n} for level 2 (a single-variable marginal).
Defaults to \code{100}.}

\item{cor_min, cor_p_max}{A level passes only when its raw-scale Pearson
correlation is at least \code{cor_min} (default \code{0.3}) with \code{p < cor_p_max}
(default \code{0.05}).}

\item{unknown_codes}{Values in the \code{control} columns that mark an unknown
group and so cannot be assigned a slope under \code{method = "average"}.}
}
\value{
A tibble with one row per observed combination of \code{control}'s
collapsed classes (or one row overall, when \code{control} is empty): the
control columns, \code{pass_level} (\code{"cell"}, \code{"marginal"}, \code{"pooled"}, or
\code{"none"}), and \code{b_sli_avg} -- the resolved slope, named to match
\code{\link[=build_sli_slopes_tbl]{build_sli_slopes_tbl()}}'s output so both feed \code{\link[=calc_sli]{calc_sli()}}\code{(control = ...)} identically. \code{NA} wherever \code{pass_level} is \code{"none"}.
}
\description{
An alternative to \code{\link[=build_sli_slopes_tbl]{build_sli_slopes_tbl()}} for when some \code{control} cells
are too sparse or too weakly correlated to fit their own SMA slope. Rather
than averaging every \code{control} variable's marginal slope unconditionally,
this resolves one slope per cell from a cascade, finest to coarsest,
stopping at the first level that clears both a minimum sample size and a
reliable mass-appendage correlation (\code{cor_min}, \code{cor_p_max}):
}
\details{
\enumerate{
\item \strong{The cell itself} -- the full cross of both \code{control} variables (only
possible, and only attempted, with exactly two). Gated by \code{n_min_cell}.
\item \strong{Single-variable marginals} -- each \code{control} variable's own slope,
pooling across the other. Gated by \code{n_min_marginal}; averaged together
when both pass, used unblended when only one does.
\item \strong{The pooled slope} -- fit on every row of \code{df}, regardless of \code{control}.
Reached when no finer level passes, and used for every cell when
\code{control} is empty.
}

\code{df}'s pooled mass-appendage correlation gates the whole cascade: when it
is not itself reliable, every cell's slope is \code{NA} (\code{pass_level = "none"})
rather than falling back to an unreliable pooled fit. This keeps a caller's
downstream SLI defined for either all of \code{df}'s rows or none, matching the
shared-N convention other body-size-correction methods need for a fair
comparison across them.

Unlike \code{\link[=build_sli_slopes_tbl]{build_sli_slopes_tbl()}} and \code{\link[=build_group_cor_tbl]{build_group_cor_tbl()}}, rows whose
\code{control} values are \code{NA} or match \code{unknown_codes} are \strong{not} dropped:
they are collapsed to an explicit \code{"Unk"} class per variable, which
cascades through the same three levels as every other class -- not a
special case or an automatic fallback to pooled.
}
\examples{
set.seed(1)
d <- sim_allometric(n = 500)
d$Age <- sample(c("Juvenile", "Adult", NA), nrow(d), replace = TRUE, prob = c(.1, .7, .2))
d$Sex <- sample(c("F", "M"), nrow(d), replace = TRUE)
build_sli_slopes_hierarchical(d, control = c("Age", "Sex"))

}
\seealso{
\code{\link[=build_sli_slopes_tbl]{build_sli_slopes_tbl()}}, the unconditional-averaging alternative
this generalises; \code{\link[=build_group_cor_tbl]{build_group_cor_tbl()}} for the same per-cell
reliability diagnostic without the cascade.
}
