% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/slopes.R
\name{build_sli_slopes_tbl}
\alias{build_sli_slopes_tbl}
\title{Per-group SMA allometric slopes}
\usage{
build_sli_slopes_tbl(
  df,
  Append = Append,
  Mass = Mass,
  control,
  unknown_codes = c("Unk", "U", "Unknown"),
  slope_diff_warn = 0.15
)
}
\arguments{
\item{df}{A data frame of individual measurements.}

\item{Append, Mass}{Unquoted column names for appendage length and body mass.
\strong{These default to columns literally named \code{Append} and \code{Mass}.} If your
columns are named otherwise, pass them explicitly (e.g. \verb{Append = wing, Mass = mass}); an informative error is raised if the columns are absent.}

\item{control}{Character vector of grouping columns. Each must be character
or factor.}

\item{unknown_codes}{Values in the \code{control} columns that mark an unknown
group and so cannot be assigned a slope under \code{method = "average"}.}

\item{slope_diff_warn}{Warn when the per-variable SMA slopes being averaged
within a cell span more than this. Defaults to \code{0.15}, roughly half the
isometric exponent. \code{NULL} or \code{Inf} disables the check. Has no effect with
a single \code{control} variable, where nothing is averaged.}
}
\value{
A tibble with one row per observed combination of \code{control} levels:
the control columns, \code{n} (rows contributing to that cell), one
\verb{b_sma_<var>} column per control variable, and their row mean
\code{b_sli_avg}. A cell whose slope is missing for any control variable gets
\code{b_sli_avg = NA}.
}
\description{
Fits one standardised major axis (SMA) regression of \code{log(Append)} on
\code{log(Mass)} per \code{control} variable, letting the slope vary across that
variable's levels. Every combination of levels present in the data then
receives \code{b_sli_avg}, the mean of the slopes its levels attract.
}
\details{
Note the structure: with \code{control = c("Age", "Sex")} this fits \strong{two}
models — one interacting mass with age, one interacting mass with sex — and
averages their slopes per age × sex cell. It does not fit a single
age × sex interaction (which is not possible in the smatr package). That
keeps the per-cell sample sizes at the marginal rather than the joint level,
which matters when some cells are sparse.

The cost of that averaging is that a cell whose control variables disagree
about the slope is collapsed to their midpoint, and nothing in \code{b_sli_avg}
records the disagreement. \code{slope_diff_warn} guards against this: when any
cell's per-variable slopes span more than that much, a warning names the
worst cell and its spread. Inspect the \verb{b_sma_<var>} columns before trusting
\code{b_sli_avg}, and consider correcting on one control variable at a time.

Rows whose \code{control} values are \code{NA} or match \code{unknown_codes} are excluded
from slope fitting and from the returned table.
}
\examples{
set.seed(1)
d <- sim_allometric(n = 400)
d$Sex <- rep(c("F", "M"), length.out = nrow(d))
build_sli_slopes_tbl(d, control = "Sex")

}
\seealso{
\code{\link[=build_group_cor_tbl]{build_group_cor_tbl()}}, which flags groups whose mass–appendage
relationship is too weak for a per-group slope to be meaningful.
}
