% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/solve.R
\name{implied_gradient_effect}
\alias{implied_gradient_effect}
\title{Resolve the ground-truth effect of a gradient on relative appendage size}
\usage{
implied_gradient_effect(
  r_app_mass = NULL,
  b_ols = NULL,
  b_sma = NULL,
  b_avg = NULL,
  r_grad_app,
  r_grad_mass,
  b_anchor = 1/3,
  sd_log_append = NULL,
  sd_log_mass = NULL
)
}
\arguments{
\item{r_app_mass}{Pearson correlation between \code{log(Append)} and \code{log(Mass)}.
Must be non-zero.}

\item{b_ols, b_sma, b_avg}{The OLS, SMA, and midpoint slopes of \code{log(Append)}
on \code{log(Mass)}.}

\item{r_grad_app}{Pearson correlation between the gradient and \code{log(Append)}.}

\item{r_grad_mass}{Pearson correlation between the gradient and \code{log(Mass)}.}

\item{b_anchor}{The scaling exponent defining "no relative change" -- the
reference \code{\link[=calc_sli]{calc_sli()}}-style methods are implicitly or explicitly scored
against. Defaults to \code{1/3}, the isometric exponent for a linear appendage
against a volumetric body-size proxy like mass.}

\item{sd_log_append, sd_log_mass}{Log-scale standard deviations.}
}
\value{
A one-row tibble: the resolved allometry (as \code{\link[=implied_allometry]{implied_allometry()}}),
\code{r_grad_app}, \code{r_grad_mass}, \code{b_anchor}, \code{beta_ref}, and its decomposition
\code{allometry_component} + \code{differential_component} (which sum to \code{beta_ref}
exactly, up to floating-point error).
}
\description{
Extends \code{\link[=implied_allometry]{implied_allometry()}} to a third variable -- an environmental or
temporal gradient along which both appendage length and mass may covary
(temperature, latitude, year...). Answers the question a simulation needs
before it can score any estimator: \emph{given this allometry and these
gradient correlations, what relative change should a correctly-specified
estimator recover?}
}
\details{
The appendage-mass allometry is resolved exactly as in \code{\link[=implied_allometry]{implied_allometry()}}
(supply any two of \code{r_app_mass}, \code{b_ols}, \code{b_sma}, \code{b_avg}, or none for the
isometric default). The gradient enters through two further correlations,
\code{r_grad_app} and \code{r_grad_mass} -- \code{\link[=sim_allometric]{sim_allometric()}}'s own names for a
gradient's correlation with \code{log(Append)} and \code{log(Mass)} respectively.

Relative appendage size is defined against \code{b_anchor}: the scaling exponent
that counts as "no relative change". \code{beta_ref} is the reference effect
size a method anchored at \code{b_anchor} should report:

\deqn{\beta_{ref} = \rho_{grad,app}\, b_{sma} - b_{anchor}\, \rho_{grad,mass}}

Writing \eqn{\rho_{grad,app} = \rho_{grad,mass} + \Delta\rho}, this splits
exactly into two components, returned separately because they answer
different questions about \emph{why} \code{beta_ref} is non-zero:

\deqn{\beta_{ref} = \underbrace{\rho_{grad,mass}(b_{sma} - b_{anchor})}_{\text{allometry\_component}} +
  \underbrace{b_{sma}\,\Delta\rho}_{\text{differential\_component}}}

\code{allometry_component} is non-zero whenever the realised allometry departs
from the anchor, even with no differential association between the
gradient and either trait (\code{r_grad_app = r_grad_mass}). \code{differential_component}
is non-zero only when the gradient associates more strongly with one trait
than the other. An anchor fixed a priori (e.g. isometry, \code{b_anchor = 1/3})
is sensitive to both; anchoring at the realised allometry itself
(\code{b_anchor = b_sma}) zeroes \code{allometry_component} identically, so \code{beta_ref}
can only reflect differential association. That is the distinction between
anchoring \code{\link[=calc_sli]{calc_sli()}} at a fixed exponent versus at each group's own
estimated slope (\verb{control =}), and it changes what a significant \code{beta_ref}
can be taken as evidence of.
}
\examples{
# Isometric anchor: both components can contribute.
implied_gradient_effect(b_sma = 0.5, r_app_mass = 0.3,
                         r_grad_app = 0.2, r_grad_mass = -0.1)

# Anchored at the realised allometry itself: allometry_component vanishes,
# matching SLI-estimated's logic (calc_sli(control = ...)).
implied_gradient_effect(b_sma = 0.5, r_app_mass = 0.3,
                         r_grad_app = 0.2, r_grad_mass = -0.1, b_anchor = 0.5)

}
\seealso{
\code{\link[=implied_allometry]{implied_allometry()}}, which this extends to a gradient;
\code{\link[=sim_allometric]{sim_allometric()}} to simulate data with this exact correlation structure.
}
