% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sli.R
\name{calc_lambda}
\alias{calc_lambda}
\title{Ratio of squared coefficients of variation}
\usage{
calc_lambda(x, y, na.rm = FALSE)
}
\arguments{
\item{x, y}{Numeric vectors of equal length. Both must be strictly positive
for the coefficient of variation to be meaningful.}

\item{na.rm}{Drop missing values before computing means and standard
deviations.}
}
\value{
A single numeric value.
}
\description{
Computes \eqn{\lambda = CV(y)^2 / CV(x)^2}, the empirical coefficient
relating the variance of two traits on the log scale. With \code{x} a body mass
and \code{y} an appendage length, \eqn{\sqrt{\lambda}} is the standardised major
axis (SMA) slope, so \code{calc_lambda()} gives a quick moment-based check on a
fitted allometric exponent without fitting a model.
}
\examples{
set.seed(1)
d <- sim_allometric(n = 500, b_avg = 0.33)
calc_lambda(x = d$Mass, y = d$Append)

}
