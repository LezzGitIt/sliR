% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate.R
\name{sim_grid}
\alias{sim_grid}
\title{Simulate \code{\link[=sim_allometric]{sim_allometric()}} across a grid of parameters}
\usage{
sim_grid(params, ..., id_col = ".scenario")
}
\arguments{
\item{params}{A data frame, one row per scenario, with columns named after
\code{\link[=sim_allometric]{sim_allometric()}}'s arguments (e.g. \code{n}, \code{r_app_mass}, \code{r_grad_app}).}

\item{...}{Arguments passed to every call of \code{\link[=sim_allometric]{sim_allometric()}}, constant
across scenarios (e.g. \code{gradient = "Temperature"}).}

\item{id_col}{Name of the integer column identifying which row of \code{params}
produced each simulated row, matching that row's position in \code{params}.
Defaults to \code{".scenario"}.}
}
\value{
A single tibble: the row-bound output of \code{\link[=sim_allometric]{sim_allometric()}} for
every row of \code{params}, with \code{id_col} added. Join \code{params} back on by row
number (\code{dplyr::mutate(params, .scenario = dplyr::row_number())}) to
recover the parameter values behind each scenario.
}
\description{
A thin wrapper for the common case of comparing many scenarios: each row of
\code{params} becomes one call to \code{\link[=sim_allometric]{sim_allometric()}}, its columns matched to
\code{sim_allometric()}'s arguments by name exactly as \code{purrr::pmap()} would.
Arguments that are constant across every scenario — most often \code{gradient},
a single string naming one column — are passed via \code{...} instead of
appearing in \code{params}.
}
\examples{
grid <- expand.grid(n = 200, r_app_mass = c(0.1, 0.3, 0.5), b_sma = 1 / 3)
set.seed(1)
sims <- sim_grid(grid)
dplyr::count(sims, .scenario)

# Arguments constant across the grid, like the gradient's name and range,
# are passed once via `...` rather than repeated in every row of `params`.
grad_grid <- expand.grid(n = 200, r_grad_app = c(-0.3, 0, 0.3), r_grad_mass = -0.1)
set.seed(1)
grad_sims <- sim_grid(grad_grid, gradient = "Temperature", gradient_range = c(0, 24))

}
\seealso{
\code{\link[=sim_allometric]{sim_allometric()}}, which this maps over.
}
