% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/slopes.R
\name{build_group_cor_tbl}
\alias{build_group_cor_tbl}
\title{Per-group mass–appendage correlation diagnostic}
\usage{
build_group_cor_tbl(
  df,
  Append = Append,
  Mass = Mass,
  control,
  unknown_codes = c("Unk", "U", "Unknown")
)
}
\arguments{
\item{df}{A data frame of individual measurements.}

\item{Append, Mass}{Unquoted column names for appendage length and body mass.
\strong{These default to columns literally named \code{Append} and \code{Mass}.} If your
columns are named otherwise, pass them explicitly (e.g. \verb{Append = wing, Mass = mass}); an informative error is raised if the columns are absent.}

\item{control}{Character vector of grouping columns. Each must be character
or factor.}

\item{unknown_codes}{Values in the \code{control} columns that mark an unknown
group and so cannot be assigned a slope.}
}
\value{
A tibble with one row per observed combination of \code{control} levels:
the control columns, \code{n}, \code{b_ols} (slope of mass on appendage), \code{r}
(Pearson correlation), and \code{p_value}.
}
\description{
Reports, for each combination of \code{control} levels, the OLS regression of
mass on appendage length together with its correlation and p-value. Use it
before \code{\link[=calc_sli]{calc_sli()}}\code{(control = ...)}: a group whose mass and appendage
length are uncorrelated has no meaningful allometric relationship, so the
SMA slope fitted to it is noise and should not be used to size-correct its
members.
}
\examples{
set.seed(1)
d <- sim_allometric(n = 400)
d$Sex <- rep(c("F", "M"), length.out = nrow(d))
build_group_cor_tbl(d, control = "Sex")

}
